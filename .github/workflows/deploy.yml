name: ftp-deploy

on:
  push:
    branches:
      - main         # 👈 Otomatik deploy için eklendi
  workflow_dispatch: # 👈 Manuel tetikleme yine aktif

jobs:
  deploy:
    name: 🚀 Deploy to CPanel
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v3

      - name: 🚀 Deploy via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASS }}
          server-dir: /     # 📌 CPanel dizini (root)
          local-dir: ./                # 📌 Replit projenin kökü (client + server + shared)
